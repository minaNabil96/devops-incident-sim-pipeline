# Google Gemini API Configuration (OpenAI-compatible endpoint)
# Get your API key from: https://aistudio.google.com/apikey
GEMINI_API_KEY=your_gemini_api_key_here

# Optional: Override default model
# GEMINI_MODEL=gemini-3.7-flash

# Optional: Override API endpoint
# GEMINI_BASE_URL=https://generativelanguage.googleapis.com/v1beta/openai/chat/completions

# --- OrcaRouter fallback (optional) ---------------------------------------
# When the Gemini free-tier daily quota is exhausted (or Gemini is down),
# the client transparently retries against OrcaRouter, an OpenAI-compatible
# gateway. Get a key (starts with sk-orca-) from https://www.orcarouter.ai
# Leave ORCAROUTER_API_KEY unset to disable the fallback entirely.
# ORCAROUTER_API_KEY=your_orcarouter_api_key_here
# ORCAROUTER_MODEL=deepseek/deepseek-v4-flash-free
# ORCAROUTER_BASE_URL=https://api.orcarouter.ai/v1/chat/completions
# ENABLE_LLM_FALLBACK=true