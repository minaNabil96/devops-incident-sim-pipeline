# Runtime dependencies (Streamlit Community Cloud / HF — light, no CrewAI).
# CrewAI is optional: the phase-by-stage runner calls the LLM directly.
# For the full multi-agent (CrewAI) stack, install requirements-full.txt instead.
streamlit>=1.28.0
fpdf2>=2.8.0
requests>=2.31.0
jinja2>=3.1.2
pydantic>=2.0.0
python-dotenv>=1.0.0